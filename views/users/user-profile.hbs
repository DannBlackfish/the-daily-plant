{{!-- this.message (mensaje regar o no planta)
this.userinseesion (sesión de usuario) -> this.userInSession.name (de esta manera se obtiene el nombre)
this.info (información de plantas) -> información de plantas que tiene objetos this.info.name

{{#each this.info}}
{{this.name}}
{{this.id}}
{{/each}}

Crear una ruta que reciba el parámetro router.get(/info/:id,(req,res,next)=>{
  const id = req.params.id
  Info.findById(id)
  .then((infoFromDB)=>{
    res.render('info/detail', {info:infoFromDB})
  })
}) --}}



<div id="carouselExampleCaptions" class="carousel slide" data-bs-ride="carousel">
  <div class="carousel-indicators">
    <button type="button" data-bs-target="#uno" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
    <button type="button" data-bs-target="#dos" data-bs-slide-to="1" aria-label="Slide 2"></button>
    <button type="button" data-bs-target="#tres" data-bs-slide-to="2" aria-label="Slide 3"></button>
  </div>

  <div class="carousel-inner">

    <div class="carousel-item active" data-bs-target="#uno">
      <img src="images/tomato.png" class="d-block w-100" alt="...">
      <div class="carousel-caption d-none d-md-block">
        {{#each this.info}}
        <h5>{{this.name}}</h5>
        <a method="GET" href="/info/detail/{{this.id}}"><p>Information</p></a>
        {{/each}}
      </div>
    </div>

    <div class="carousel-item" data-bs-target="#dos">
      <img src="images/peach.png" class="d-block w-100" alt="...">
      <div class="carousel-caption d-none d-md-block">
        <h5>Second slide label</h5>
        <p>Some representative placeholder content for the second slide.</p>
      </div>
    </div>

    <div class="carousel-item">
      <img src="..." class="d-block w-100" alt="...">
      <div class="carousel-caption d-none d-md-block">
        <h5>Third slide label</h5>
        <p>Some representative placeholder content for the third slide.</p>
      </div>
    </div>

  </div>

  <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions"  data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
  </button>
  <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions"  data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
  </button>
</div>


<div class="card text-center">
  <div class="card-header">
    Featured
  </div>
  <div class="card-body">
    <h5 class="card-title">{{this.name}}</h5>
    <p class="card-text">{{this.message}}</p>
    <form >
        <a href="/info/new" class="btn btn-primary">Add Plants</a>
    </form>
  </div>
  <div class="card-footer text-muted">
    2 days ago
  </div>
  {{!-- <div>
    <a action="/info/delete/{{id}}" method="POST" type="submit">Delete Plant</a>
  </div> --}}
</div>

{{!-- {{#if userInSession}}
    <h2>Welcome, {{ userInSession.username }}!</h2>
    <p>This is your profile page my friend!</p>
    <button>
        <a href="/userProfile">Reload the page</a>
    </button>
{{else}}
    <p>Still no logged in user, sorry!</p>
{{/if}} --}}

<script>
    $(document).ready(function(){
        $('.myCarousel').carousel({
            interval: 3000
        });
    });
</script>

